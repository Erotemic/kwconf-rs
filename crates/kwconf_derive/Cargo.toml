[package]
name = "kwconf_derive"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Derive macros for kwconf; use the kwconf crate instead of depending on this directly"
keywords = ["cli", "config", "derive", "kwconf"]
categories = ["command-line-interface", "config"]

[lib]
proc-macro = true

[dependencies]
proc-macro2.workspace = true
quote.workspace = true
syn.workspace = true
